import {createStyles} from '@mantine/core';

export const useStyles = createStyles(() => ({
    profile: {
        width: '100%',
        padding: 10,
    },
    profileHeader: {
        alignItems: 'flex-start',
        width: '100%',
        paddingBottom: 5,
    },
    infoGroup: {
        paddingBottom: 5,
        paddingTop: 5,
    },
}));
